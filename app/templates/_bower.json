{
  "name": "<%= _.slugify(projectName) %>",
  "version": "1.0.0",
  "dependencies": {<% if (confirmjQuery) { if (!confirmIE8) { %>
    "jquery": "~2.0.3"<% } else { %>"jquery": "~1.10.2"<% } } %>
  }
}

